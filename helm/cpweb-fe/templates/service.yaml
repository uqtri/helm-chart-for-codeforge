apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpweb-fe.fullname" . }}
  labels:
    {{- include "cpweb-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpweb-fe.selectorLabels" . | nindent 4 }}
