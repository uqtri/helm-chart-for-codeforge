apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cpweb-fe.fullname" . }}-test-connection"
  labels:
    {{- include "cpweb-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cpweb-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
